/*
  TODO: This main/modules needs to be removed
  Use the unified modules instead (root/modules)
 */

import { DatastoreModule } from './datastore.module';
import { FileSystemModule } from './file-system.module';
import { MediaModule } from './media.module';

export {
  DatastoreModule,
  FileSystemModule,
  MediaModule,
};
