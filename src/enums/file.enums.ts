export enum AudioFileExtensions {
  FLAC = 'flac',
  MP3 = 'mp3',
  M4A = 'm4a',
  WAV = 'wav',
}

export enum ImageFileExtensions {
  JPG = 'jpg',
  JPEG = 'jpeg',
  PNG = 'png',
  WEBP = 'webp',
}

export const AudioFileExtensionList = Object.values(AudioFileExtensions);

export const ImageFileExtensionList = Object.values(ImageFileExtensions);
